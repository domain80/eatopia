server:
  port: 4040
  address: localhost

spring:
  profiles:
    active: [test, dev]
  application:
    name: wolistika-backend
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org:
      springframework:
        security: TRACE
